[package]
name = "retaprompt_commands"
version = "0.1.0"
edition = "2024"
autobins = false
description = "Prompt command library layer for rp/rpl/rpe/rpb on top of reta."
license = "MIT OR Apache-2.0"
publish = false

[lib]
name = "retaprompt_commands"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
reta = { path = "../.." }
