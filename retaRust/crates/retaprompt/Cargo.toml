[package]
name = "retaprompt"
version = "0.7.0"
edition = "2024"
autobins = false
publish = false

[lib]
name = "retaprompt"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
retaprompt_commands = { path = "../retaprompt_commands" }
