# retaprompt

Legacy mixed package from the earlier transitional layout.

It is intentionally **not** part of the active workspace member list anymore.
The active split layout is now:

- `reta`
- `retaprompt_input`
- `retaprompt_commands`

The wrapper package `retaprompt_frontends` may remain in the repository as preserved old code, but it is not part of the active split.

This package is kept only as an inactive historical artifact so older experiments are not deleted.
