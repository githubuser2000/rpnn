# bitexact continue 3

Ein Projekt, weiter an Python-Kontrollfluss angenähert.

Neu in diesem Pass:
- `__runAlles`, `__invertAlles`, `__resultingTable`, `allesParameters`
- `workflowEverything(argv, words)` kehrt als eigener Schritt zurück
- `runAllesLikePythonInit(...)` ahmt den Python-Init-Pfad an
- `bringAllImportantBeginThings(...)` liefert jetzt ein Tuple-artiges Ergebnis
- `oberesMaximumArg`, `oberesMaximum2`, `oberesMaximum` sind näher an Python
